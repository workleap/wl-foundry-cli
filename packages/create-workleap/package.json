{
  "name": "create-workleap",
  "version": "0.0.0",
  "description": "Foundry-CLI Workleap project starter kit",
  "bin": "./dist/bin.js",
  "scripts": {
    "build": "tsup",
    "clean": "pnpm dlx rimraf dist coverage",
    "lint:types": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/core": "0.3.2",
    "@clack/prompts": "0.6.3",
    "picocolors": "1.0.0"
  },
  "tsup": {
    "entry": ["src/bin.ts"],
    "clean": true,
    "dts": true,
    "format": ["cjs"]
  }
}
